// Crie um código javascript que checa com estruturas condicionais (if - else) se um determinado número é par ou ímpar. Lembre-se de imprimir o resultado na tela! 

console.log("at rum");
let ah = (prompt('Informe o número por favor:'));

    const resultado = (ah/2) % 2 == 0
    let bh
    var resto = ah % 2;

    if (resto == 0) {
        bh = "número "+ah+" é par!"

    } else {
        bh = "número "+ah+" é impar!"
    }



const pagina = ["Você digitou "+ah,bh];

alert(pagina);
